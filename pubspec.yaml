name: meu_projeto
description: "A new Flutter project."

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  mobile_scanner: ^4.0.0
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^0.13.0
  share_plus: ^7.2.1         # Plugin para compartilhamento
  image_picker: ^1.0.7       # Plugin para selecionar imagens (ex: foto de perfil)
 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
